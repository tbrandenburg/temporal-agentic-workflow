// Phase 0 placeholder. POST /runs, GET /runs/:id, POST /runs/:id/cancel land in Phase 1.
export function registerRunRoutes(): void {
  // TODO(phase-1): register routes against a Fastify instance.
}
